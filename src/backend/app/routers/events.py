"""Events CRUD router — stub for future implementation."""

from fastapi import APIRouter

router = APIRouter(prefix="/events", tags=["events"])


# TODO: Implement GET /events, POST /events, GET /events/{id}, PUT /events/{id}, DELETE /events/{id}
