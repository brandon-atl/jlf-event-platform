"""Co-creator portal router — stub for future implementation."""

from fastapi import APIRouter

router = APIRouter(prefix="/portal", tags=["portal"])


# TODO: Implement GET /portal/events, GET /portal/events/{event_id}
