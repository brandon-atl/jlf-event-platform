# Production env vars — set these in Railway dashboard (Variables tab)
# DO NOT commit real values to git

# Database (Railway provides this automatically when you link Postgres)
DATABASE_URL=postgresql+asyncpg://postgres:PASSWORD@postgres.railway.internal:5432/railway

# Stripe (swap to Brian's production keys when ready)
STRIPE_SECRET_KEY=sk_live_xxx
STRIPE_PUBLISHABLE_KEY=pk_live_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx

# Email
RESEND_API_KEY=re_xxx
FROM_EMAIL=noreply@justloveforest.com

# Auth
JWT_SECRET_KEY=GENERATE_A_REAL_SECRET
JWT_ALGORITHM=HS256
JWT_EXPIRY_HOURS=24

# App — update these with actual deployment URLs
APP_ENV=production
APP_URL=https://jlf-event-platform.vercel.app
API_URL=https://api.justloveforest.com
CORS_ORIGINS=https://jlf-event-platform.vercel.app,https://app.justloveforest.com
